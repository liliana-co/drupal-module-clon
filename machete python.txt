iterar por las lineas del archivo python y sobre escribir
f = open("demofile.txt", "w")
for x in f:
  x.write("Woops! I have deleted the content!")
f.close()
#open and read the file after the appending:
f = open("demofile.txt", "r")
print(f.read())

import os.path
path=input(" enter the path\n")
for path,subdir,files in os.walk(path):
   for name in subdir:
       print os.path.join(path,name) # will print path of directories
   for name in files:    
       print os.path.join(path,name) # will print path of files


for element in os.listdir(destination):      
        if old_module_name in element:
            old_file = destination + '/' + element
            new_file = destination + '/' + element.replace(old_module_name,new_module_name)
            os.rename(old_file, new_file)

for element in glob.glob(destination+"/"+old_module_name+".*"):
        old_file = destination + '/' + element
        new_file = destination + '/' + element.replace(old_module_name,new_module_name)
        os.rename(old_file, new_file)

para crear el ejecutable
1. creo un entorno virtual: me ubico en la carpta deonde tengo mis archivos .py
   desde la consola ejecuto python -m venv . (el punto indica que se cree el entorno viertual en esa misma ruta)

activo el entorno virtual desde la consola cmd con Scripts\activate.bat o en la cosona power shell con Scripts\Activate.ps1